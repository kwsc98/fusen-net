# Local container secrets

Place development certificates, private keys, CA certificates, and generated token files in this directory. Only this README and `.gitignore` belong in version control.

Never use the development material from this directory in a public or production deployment.
